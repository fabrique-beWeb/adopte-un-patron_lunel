{% extends 'template.html.twig' %}
{% block stylesheets %}
            <link rel="stylesheet" type="text/css" href="{{ asset('../vendor/twbs/bootstrap/dist/css/bootstrap.css')}}"/>
            <link rel="stylesheet" type="text/css" href="{{ asset('./css/navbar.css')}}" />
            <link rel="stylesheet" type="text/css" href="{{ asset('./css/gestionProfilRecruteur.css')}}" />
        {% endblock %}
{% block body %}
    <div class="container-fluid">
    <div class="row">
    <div class="couleurfond col-md-12">
    <h1 class="formtitre police text-position">Recruteur edit</h1>

    {{ form_start(edit_form) }}
        <div class="col-md-6 policespecial">{{ form_row(edit_form.nom) }}
       {{ form_row(edit_form.prenom) }} </div>
        <div class="col-md-6 policespecial">{{ form_row(edit_form.email) }}
        {{ form_row(edit_form.codePostal ) }}</div>
        <div class="col-md-6 policespecial"> {{ form_row(edit_form.entreprise ) }}
        {{ form_row(edit_form.description ) }} {{ form_row(edit_form.logo) }} </div>
        <div class="col-md-6 policespecial">{{ form_row(edit_form.mdp) }}</div>
         
     <input type="submit" class="btn btn-info btn-sm text-position col-md-offset-0 col-md-1 colorbouttton pull-right" value="Edit" />
    {{ form_end(edit_form) }}
    </div>
    </div>
    </div>
    <ul>
        <li>
            <a class="lienbot" href="{{ path('indexRecruteur') }}">retour index</a>
        </li>
{#        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>#}
    </ul>
{% endblock %}
